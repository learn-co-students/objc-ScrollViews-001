
source 'https://github.com/CocoaPods/Specs'
platform :ios, '8.0'

# Add Application pods here

target 'ScrollViewAutoLayout' do

end

target :ScrollViewAutoLayoutTests, :exclusive => true do
  inhibit_all_warnings!
  link_with 'ScrollViewAutoLayoutTests'

  pod 'Specta', '~> 1.0'
  pod 'Expecta', '~> 1.0'
  pod 'KIF', '~> 3.3'
end